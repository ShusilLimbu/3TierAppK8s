apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongo-pv
  namespace: dev
spec:
  capacity:
    storage: 6Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/mongo
    type: DirectoryOrCreate
  persistentVolumeReclaimPolicy: Retain